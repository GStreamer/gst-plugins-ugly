gst-launch-0.8 dvdnavsrc ! dvddemux name=demux .current_video ! mpeg2dec ! queue max-size-buffers=0 max-size-time=0 max-size-bytes=25000000 ! { imagemixer name=i ! ffmpegcolorspace ! videoscale ! xvimagesink } demux.current_subpicture ! dvdsubdec ! queue max-size-buffers=0 max-size-bytes=0 max-size-time=0 block-timeout=10000000 ! i.subpicture_sink_%d demux.current_audio ! a52dec ! { queue ! audioconvert ! audioscale ! alsasink }
